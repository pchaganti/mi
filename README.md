# mi

30 lines. agentic repl. bash + fs. openai wire protocol.

```
OPENAI_API_KEY=sk-... node index.mjs
MODEL=qwen3.5:4b OPENAI_BASE_URL=http://localhost:33821 node index.mjs
```

model gets: `bash(cmd)` `read(path)` `write(path,content)`  
you get: a prompt and a loop.
